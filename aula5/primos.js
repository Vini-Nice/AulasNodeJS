const readline = require('readline-sync');
const num = Number(readline.question('Quantidade de primos? '));

function geradorDePrimos(let qtde){
    let counter=0, refNum=2, ehPrimo=true;

    while (counter < qtde) {
        for (let i=2 ; i < refNum/2+1 ; i++) {
            if(refNum % i)
        }
    }
}

if (isNaN(num)) {
    console.log('\nOoops, isso não é um número!');
} else {

}